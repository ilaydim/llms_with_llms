# Frontend — Learning LLMs with LLMs

```bash
npm install
npm run dev
```

Backend'in `http://127.0.0.1:8000` üzerinde çalışıyor olması gerekir
(bkz. `../backend/README.md`). API adresi `src/api.js` içinde `BASE_URL`
sabitiyle tanımlıdır.
